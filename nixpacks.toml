[phases.setup]
nixPkgs = ["nodejs_22", "python312", "gcc", "ffmpeg"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "node build.mjs",
  "python3 -m venv .venv",
  ".venv/bin/pip install -q --upgrade pip yt-dlp",
  ".venv/bin/pip install -q telethon cryptg py-tgcalls"
]

[start]
cmd = "node dist/index.mjs"
